/**
 * Website CRM — public, non-secret site configuration.
 *
 * OWNER ACTION REQUIRED:
 * Set ANDROID_APP_DOWNLOAD_URL to the official HTTPS URL of the Website CRM
 * Android APK when it is hosted. Leave empty until that URL exists.
 *
 * Do NOT put secrets, tokens, database credentials, or OAuth values here.
 * Do NOT use http://, localhost, or invented domains.
 */
window.WEBSITE_CRM_PUBLIC_CONFIG = Object.freeze({
  // Example shape only (do not use until replaced with the real HTTPS APK URL):
  // ANDROID_APP_DOWNLOAD_URL: "https://example.invalid/REPLACE_WITH_OFFICIAL_APK_URL.apk"
  ANDROID_APP_DOWNLOAD_URL: ""
});
